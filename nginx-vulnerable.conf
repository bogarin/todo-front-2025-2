# ==========================================
# ⚠️ CONFIGURACIÓN VULNERABLE DE NGINX ⚠️
# ==========================================
# ❌ NUNCA USAR EN PRODUCCIÓN ❌
# Este archivo es solo para propósitos educativos y demostración
# de vulnerabilidades de seguridad
#
# PROPÓSITO: Demostrar qué pasa cuando NO se implementan
# medidas de seguridad en Nginx
# ==========================================

server {
    listen 80;
    server_name localhost;

    # Directorio raíz donde están los archivos estáticos
    root /usr/share/nginx/html;
    index index.html;

    # Configuración de logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # ⚠️ VULNERABILIDAD #1: Compresión gzip sin protección
    # Problema: Comprime TODO, incluyendo archivos sensibles
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;

    # ⚠️ VULNERABILIDAD #2: Sin bloqueo de archivos de entorno
    # Problema: Archivos .env son accesibles públicamente
    # Impacto: Exposición de credenciales, API keys, secretos
    # Ejemplo: curl http://app.com/.env → 200 OK

    # ⚠️ VULNERABILIDAD #3: Sin bloqueo de archivos de configuración
    # Problema: package.json, docker-compose.yml accesibles
    # Impacto: Revelar dependencias vulnerables, arquitectura
    # Ejemplo: curl http://app.com/package.json → 200 OK

    # ⚠️ VULNERABILIDAD #4: Sin bloqueo de repositorio Git
    # Problema: .git/ completo es accesible
    # Impacto: Descarga del repositorio completo, historial, credenciales
    # Ejemplo: wget -r http://app.com/.git/

    # ⚠️ VULNERABILIDAD #5: Sin bloqueo de archivos ocultos
    # Problema: .htaccess, .DS_Store, .vscode/ accesibles
    # Impacto: Revelar estructura del proyecto, configuración IDE
    # Ejemplo: curl http://app.com/.htaccess → 200 OK

    # ⚠️ VULNERABILIDAD #6: Sin bloqueo de código fuente
    # Problema: Archivos .ts, .tsx, .jsx accesibles
    # Impacto: Lógica de negocio expuesta, credenciales hardcodeadas
    # Ejemplo: curl http://app.com/src/auth/login.tsx → 200 OK

    # ⚠️ VULNERABILIDAD #7: Sin bloqueo de source maps
    # Problema: Archivos .map permiten leer código sin minificar
    # Impacto: Revelar código original, comentarios, nombres de variables
    # Ejemplo: curl http://app.com/assets/index.js.map → 200 OK

    # ⚠️ VULNERABILIDAD #8: Sin bloqueo de directorios sensibles
    # Problema: node_modules/, src/, public/ accesibles
    # Impacto: Acceso a dependencias, código fuente completo
    # Ejemplo: curl http://app.com/node_modules/react/package.json → 200 OK

    # ⚠️ CONFIGURACIÓN VULNERABLE: Solo regla básica
    # Esta configuración permite acceder a CUALQUIER archivo en /usr/share/nginx/html
    # try_files intenta servir el archivo directamente si existe
    location / {
        try_files $uri $uri/ /index.html;
        
        # ⚠️ VULNERABILIDAD #9: Sin headers de seguridad
        # No hay X-Content-Type-Options, X-Frame-Options, CSP, etc.
    }

    # ⚠️ VULNERABILIDAD #10: Sin rate limiting
    # Problema: Permite fuerza bruta ilimitada
    # Impacto: Ataques DoS, escaneo de directorios

    # ⚠️ VULNERABILIDAD #11: Sin timeout configurado
    # Problema: Conexiones pueden quedar abiertas indefinidamente

    # ⚠️ VULNERABILIDAD #12: Sin validación de métodos HTTP
    # Problema: Acepta PUT, DELETE, TRACE, etc.
}

# ==========================================
# RESUMEN DE VULNERABILIDADES:
# ==========================================
# 1. ❌ Archivos .env accesibles (CRÍTICO)
# 2. ❌ Repositorio .git/ descargable (CRÍTICO)
# 3. ❌ Archivos de configuración expuestos (ALTO)
# 4. ❌ Código fuente TypeScript accesible (ALTO)
# 5. ❌ Source maps disponibles (MEDIO)
# 6. ❌ Archivos ocultos accesibles (MEDIO)
# 7. ❌ node_modules/ expuesto (MEDIO)
# 8. ❌ Sin headers de seguridad (MEDIO)
# 9. ❌ Sin rate limiting (BAJO)
# 10. ❌ Sin validación de métodos HTTP (BAJO)
#
# NIVEL DE RIESGO GLOBAL: 🔴 CRÍTICO (95/100)
# ==========================================

# ==========================================
# ARCHIVOS QUE PUEDEN SER ACCEDIDOS:
# ==========================================
# curl http://localhost/.env                          → 200 OK ❌
# curl http://localhost/.env.local                    → 200 OK ❌
# curl http://localhost/.env.production               → 200 OK ❌
# curl http://localhost/.git/config                   → 200 OK ❌
# curl http://localhost/.git/HEAD                     → 200 OK ❌
# curl http://localhost/.git/logs/HEAD                → 200 OK ❌
# curl http://localhost/package.json                  → 200 OK ❌
# curl http://localhost/package-lock.json             → 200 OK ❌
# curl http://localhost/tsconfig.json                 → 200 OK ❌
# curl http://localhost/vite.config.ts                → 200 OK ❌
# curl http://localhost/docker-compose.yml            → 200 OK ❌
# curl http://localhost/src/main.tsx                  → 200 OK ❌
# curl http://localhost/src/api/todosApi.ts           → 200 OK ❌
# curl http://localhost/src/auth/keycloak.ts          → 200 OK ❌
# curl http://localhost/assets/index.js.map           → 200 OK ❌
# curl http://localhost/node_modules/react/index.js   → 200 OK ❌
# curl http://localhost/.htaccess                     → 200 OK ❌
# curl http://localhost/.DS_Store                     → 200 OK ❌
# curl http://localhost/.vscode/settings.json         → 200 OK ❌
# curl http://localhost/README.md                     → 200 OK ❌
# curl http://localhost/Dockerfile                    → 200 OK ❌
# ==========================================

# ==========================================
# PARA USAR ESTA CONFIGURACIÓN VULNERABLE:
# ==========================================
# 1. Construir imagen con esta config:
#    docker build -f Dockerfile.vulnerable -t todo-front:vulnerable .
#
# 2. Ejecutar contenedor vulnerable:
#    docker run -d -p 8081:80 --name todo-vulnerable todo-front:vulnerable
#
# 3. Probar vulnerabilidades:
#    ./test-security.sh http://localhost:8081
#
# 4. Comparar con versión segura:
#    docker run -d -p 8080:80 --name todo-secure todo-front:latest
#    ./test-security.sh http://localhost:8080
# ==========================================
